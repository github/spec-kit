# Example Workspace Configuration
# Auto-generated by init-workspace.sh

workspace:
  name: attun-project
  root: /Users/hubert_1/git/attun-project
  version: 1.0.0

repos:
  - name: attun-backend
    path: ./attun-backend
    aliases: [attun, backend]

  - name: attun-frontend
    path: ./attun-frontend
    aliases: [attun, frontend]

conventions:
  # Prefix-based routing: spec name starts with prefix
  prefix_rules:
    backend-: [attun-backend]
    frontend-: [attun-frontend]
    fullstack-: [attun-backend, attun-frontend]
    api-: [attun-backend]
    ui-: [attun-frontend]

  # Suffix-based routing: spec name ends with suffix
  suffix_rules:
    -api: [attun-backend]
    -backend: [attun-backend]
    -ui: [attun-frontend]
    -frontend: [attun-frontend]
    -web: [attun-frontend]

  # Defaults for ambiguous cases
  defaults:
    ambiguous_prompt: true  # Prompt user when multiple repos match
    default_repo: null      # No default - must match or prompt
